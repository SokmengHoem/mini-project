spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mini-project
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
